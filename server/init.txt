pra certificado https o tunel se puede utilizar ngrock : https://dashboard.ngrok.com/
pas: RUXLJ44LMV6YVD72ZUQJZQOPMF6ZKL2Z  , se jecuta: ./ngrok http 3001  o .\ngrok http 3001

1) npm init -y
2) npm i express nodemon axios morgan sequelize pg-hstore dotenv cors cookie-parser pg 
   .npm i jsonwebtoken
   .
3) crear index.js fuera de carpeta src osea en la raiz del 
   proyecto.
4) se crea app dentro del src,se llama server, const  
express = require('express');se exporta module.export =app
4) en los scritps crear comando "start":"nodemon index.js", tambien: "type": "module"
5) me voy a mi index.js y llamo a mi app dentro de src, const  
app= require('./src/app') 
y app.listen(3001, ()=>{  console.log('listening on puerto 3001')} )
6) configuro morgan como dev en app, app.use(morgan("dev"))
 5.2)en app creo, app.get(("/"),(req,res)=>{res.status(200).send("Ok")}), 
 debe funcionar el server
6) configurar mi server en el index.js y llamo al index.js de las rutas
7) ya se pueden crear las demas rutas qie se utilizaran

.require('dotenv').config(); const { DB_HOST} = process.env;